apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: app
  labels:
    app: nodeapp
    component: app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nodeapp
      component: app
  template:
    metadata:
      name: app-pod
      labels:
        app: nodeapp
        component: app
    spec:
      containers:
      - image: gin/nodeapp
        name: nodeapp
        env:
        - name: COMMENT_DATABASE_HOST
          value: mongodb